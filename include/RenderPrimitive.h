#ifndef RENDER_PRIMITIVE_H
#define RENDER_PRIMITIVE_H

class RenderPrimitive
{
public:
    RenderPrimitive();
    ~RenderPrimitive();
    bool Initialize();
    void Clear();
};

#endif // RENDER_PRIMITIVE_H
